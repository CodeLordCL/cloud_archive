<%= form_with(model: user_file) do |form| %>
  <% if user_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_file.errors.count, "error") %> prohibited this user_file from being saved:</h2>

      <ul>
      <% user_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label>Выбрать файл для отправки:</label><br />
    <%= form.file_field :file_image %><br />
  </div>

  <div>
    <%= form.submit 'Загрузить файл в систему' %>
  </div>

  <%= form.hidden_field :user_id, {value: current_user.id} %>
<% end %>
